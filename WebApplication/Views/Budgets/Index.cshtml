@model IEnumerable<WebApplication.Models.Budgets>

@{
    ViewData["Title"] = "Бюджет";
}

<h3>Бюджет</h3>

<p>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Бюджет")
            </th>
            <th>
                @Html.DisplayName("Процент")
            </th>
            <th>
                @Html.DisplayName("Бонус")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Budgetamount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalePercentage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bonus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редактировать</a> |
               @* <a asp-action="Details" asp-route-id="@item.Id">Details</a> |*@
                <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
